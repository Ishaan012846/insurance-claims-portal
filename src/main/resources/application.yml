spring:
  application:
    name: insurance-claims-portal

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/claims_db}
    username: ${SPRING_DATASOURCE_USERNAME:claims_user}
    password: ${SPRING_DATASOURCE_PASSWORD:claims_password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    expiration-ms: 86400000 # 24 hours
  storage:
    upload-dir: ${STORAGE_PATH:./uploads}

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
    com.example.claims: DEBUG
